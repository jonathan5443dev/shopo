import styled from 'styled-components/native';
import theme from '../../config/themes/default';

export const FavoriteButtonContainer = styled.View``;

export const FavoriteButtonImage = styled.Image``;
